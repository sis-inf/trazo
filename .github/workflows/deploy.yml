name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # Cada proyecto define sus pasos aquí
      - name: Deploy
        run: echo "Definir pasos de despliegue"